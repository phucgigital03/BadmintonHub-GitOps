# Default cho charts/service — MỌI values/<svc>-<env>.yaml đều override nameOverride/image/port/probe.
#
# ⚠️ nameOverride là BẮT BUỘC khai ở values file.
# Tên Deployment/Service KHÔNG lấy theo .Release.Name: Day 6 ArgoCD đặt release name = <svc>-<env>
# (vd user-service-staging) → Service sẽ thành "user-service-staging" và khi đó
#   · EUREKA_URL trỏ eureka-server.<ns>.svc.cluster.local → NXDOMAIN (mất service discovery)
#   · nginx trong image FE proxy /api sang host "api-gateway" → 502
#   · Ingress Day 4 khai backend.service.name: api-gateway / frontend → không match
nameOverride: ""

image:
  repository: ""
  tag: ""
  # IfNotPresent: trên kind image được nạp bằng `kind load docker-image`, KHÔNG có trên registry.
  # Always sẽ khiến kubelet đi hỏi Docker Hub → ErrImagePull dù image đã nằm sẵn trong node.
  pullPolicy: IfNotPresent

# Demo ephemeral: 1 replica mọi service. Không tạo PodDisruptionBudget ở posture này —
# minAvailable:1 trên Deployment 1 replica chặn vĩnh viễn mọi drain tự nguyện.
replicaCount: 1

# RollingUpdate với 1 replica ⇒ maxUnavailable=0 ⇒ pod CŨ phải sống tới khi pod MỚI Ready
# ⇒ hai JVM cùng chạy trong lúc rollout, đúng lúc pod mới cần CPU nhất để boot.
#   dev (kind, RAM/CPU chật): Recreate — chấp nhận gián đoạn vài chục giây, đổi lấy boot nhanh.
#   staging/prod: RollingUpdate — không gián đoạn trước mặt người dùng.
strategy: RollingUpdate

port: 8080

# PATH ĐẦY ĐỦ — chart không tự nối chuỗi.
#
# 🔴 KHÔNG dùng /actuator/health/{liveness,readiness}. Đã đo trên kind: SecurityConfig của app
# chỉ permitAll đúng 2 path LITERAL là /actuator/health và /actuator/info, nên mọi sub-path trả
# 403 (không phải 404 — /actuator/bat-ky-cai-gi cũng 403) và pod KHÔNG BAO GIỜ Ready.
# Đúng với cả 7 service Java có Spring Security; eureka-server thoát vì không có security.
#
# Cách thay thế giữ nguyên tinh thần "đừng restart pod vì datastore nhấp nháy":
#   liveness  = /actuator/info   không chạm db/redis/mongo/eureka → chỉ fail khi JVM chết/treo
#   readiness = /actuator/health composite. Dùng composite cho READINESS là AN TOÀN: Redis chết
#               thì pod bị rút khỏi Endpoints chứ KHÔNG bị restart, nên không có cascade.
# (Muốn dùng đúng nhóm liveness/readiness của Spring Boot thì phải permit /actuator/health/**
#  trong SecurityConfig ở repo app rồi build lại 8 image — xem TODO trong CLAUDE.md.)
livenessPath: /actuator/info
readinessPath: /actuator/health

# 60 × 5s = 300s cho JVM boot. Đo thật ở Day 1 (docker-compose.app.yml): một Spring web context
# mất 128s để khởi tạo trên máy 8GB, và compose phải đặt start_period: 300s.
# failureThreshold 30 (150s) sẽ giết pod giữa lúc đang boot hợp lệ.
startupFailureThreshold: 60

# 🔴 KHÔNG để mặc định. `timeoutSeconds` mặc định của K8s là 1 GIÂY — actuator của Spring Boot
# dưới sức ép CPU/RAM thường trả lời chậm hơn thế, và probe timeout bị tính y như probe fail.
# Hậu quả đúng bằng cái mà việc tách liveness/readiness sinh ra để tránh: K8s giết pod đang
# KHOẺ, pod restart lại làm cụm thêm tải → cascade.
# Đã dính thật trên kind: eureka-server và api-gateway bị giết với
#   "Liveness probe failed: context deadline exceeded (Client.Timeout exceeded while awaiting headers)"
# Không phải chuyện riêng của kind: node EKS t3.xlarge cũng phải gánh staging + prod + observability.
probeTimeoutSeconds: 5

# Liveness = "app có còn sống không", KHÔNG phải "app có đang nhanh không".
# 6 × 10s = 60s không trả lời mới restart. Vẫn bắt được treo thật (deadlock, JVM đứng), nhưng
# không giết pod chỉ vì một đợt GC dài hay lúc node đang bận boot service khác.
# Readiness thì giữ nhạy (3) — đó mới là cái nên rút pod khỏi Service khi nó chậm.
livenessFailureThreshold: 6
readinessFailureThreshold: 3

# Cả hai optional — frontend không có cái nào (nginx, không đọc env Eureka/DB).
envFrom:
  configMap: ""
  secret: ""

# ─────────────────────────── Hàng rào khởi động (Day 6) ───────────────────────────
# initContainer chờ mọi datastore mở cổng rồi mới cho JVM chạy. Danh sách host:port lấy từ biến
# DATASTORE_WAIT trong ConfigMap app-config (charts/platform ghép sẵn theo dataNamespace).
#
# 🔴 Vì sao có: đo thật ở Day 6 trên EKS — ArgoCD tạo 18 pod service lúc 02:33:31 còn 10 pod
# datastore mãi 02:36:03, sớm hơn 2 phút rưỡi. Spring chết ngay lúc boot với
# `UnknownHostException: postgresql.data-staging.svc.cluster.local` (DNS chưa có — KHÔNG phải
# connection refused) → container exit → kubelet restart. 6/9 service mỗi env restart 1-3 lần.
#
# sync-wave của ArgoCD KHÔNG chặn được việc này: nó mở cổng wave khi resource của wave trước
# Healthy, mà một Application vừa được tạo thì chưa quản lý resource nào — và app không có
# resource nào được chấm là Healthy ⇒ cả 3 wave qua hết trong ~3 giây. sync-wave là
# best-effort, KHÔNG phải hàng rào. Hàng rào thật phải nằm ở tầng kubelet.
#
# default true = an toàn. Đặt false cho service KHÔNG chạm datastore nào (eureka-server,
# frontend) — chúng phải lên sớm nhất có thể vì 7 service kia đăng ký vào Eureka lúc boot.
waitForDatastores: true

# ECR Public thay vì Docker Hub: node kéo image không đụng rate-limit ẩn danh của Docker Hub
# (100 pull/6h mỗi IP) và đi trong mạng AWS nên nhanh hơn. busybox có sẵn `nc` và `date`.
waitImage: public.ecr.aws/docker/library/busybox:1.36

# ⚠️ Ngân sách chờ CÓ CHỦ ĐÍCH. Hết giờ thì initContainer thoát 0 và để app tự thử (tức lùi về
# đúng hành vi cũ). Không có timeout thì một cổng bị NetworkPolicy chặn — đã từng xảy ra thật
# với RabbitMQ 61613 ở Day 2 — sẽ làm pod kẹt `Init:0/1` VĨNH VIỄN. Đó là hỏng nặng hơn
# crashloop: crashloop ít ra còn tự khỏi và còn log của app để đọc.
waitTimeoutSeconds: 300

# Biến riêng của service (nếu có). Hầu hết biến non-secret đã nằm ở ConfigMap app-config
# dùng chung của charts/platform, nên block này thường rỗng.
#
# ⚠️ Chỗ hay dùng nhất là ghi đè JAVA_TOOL_OPTIONS. Image bake sẵn MaxRAMPercentage=75, nghĩa là
# heap lấy 75% limit và phần CÒN LẠI phải đủ cho metaspace + thread stack + code cache + direct
# buffer (Spring Boot cần ~150-200Mi). Với limit 448Mi thì 25% chỉ còn 112Mi → OOMKilled.
# Đã dính thật ở Day 2: user-service Terminated/OOMKilled ở limit 448Mi.
env: {}

resources:
  requests:
    memory: 128Mi
    cpu: 100m
  limits:
    memory: 448Mi

# ─────────────────────────────── Metrics (Day 7) ───────────────────────────────
# Gắn label `badminton.io/metrics: enabled` lên Service. ServiceMonitor của chart observability/
# chọn theo đúng label này, nên đây là CÔNG TẮC quyết định service có bị Prometheus scrape không.
#
# default true vì 8/9 service là Spring Boot và đều có /actuator/prometheus (từ Day 7).
#
# 🔴 Đặt false cho frontend. Nó là nginx — không có actuator, scrape sẽ ra một target DOWN
# VĨNH VIỄN. Không hỏng gì về vận hành, nhưng hỏng thứ đắt hơn: một bảng target lúc nào cũng có
# màu đỏ thì vài tuần sau không ai còn nhìn nó nữa, và lúc đó target đỏ THẬT cũng chìm luôn.
# Cùng lý do với việc tắt 4 target control-plane của EKS trong observability/values.yaml.
#
# Chọn bằng LABEL chứ không bằng annotation `prometheus.io/scrape` vì Prometheus Operator làm
# việc qua ServiceMonitor + label selector; annotation kiểu cũ chỉ có tác dụng với Prometheus
# cấu hình tay, ở đây sẽ bị bỏ qua TRONG IM LẶNG.
metrics:
  enabled: true

service:
  type: ClusterIP

  # Rỗng với 7/9 service. Chỉ 2 service nằm sau ALB mới cần: api-gateway và frontend
  # (xem values/<svc>-staging.yaml). Trên kind không có ALB Controller nên annotation này
  # chỉ là metadata vô hại — giữ cùng một chart cho cả 3 env.
  annotations: {}

podAnnotations: {}
nodeSelector: {}
tolerations: []
affinity: {}
